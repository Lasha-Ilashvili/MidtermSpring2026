UNO_DATABASE_NAME=uno
UNO_DATABASE_USERNAME=uno
# Optional for external password-protected databases.
# The default Docker Compose database uses trust auth on its private network.
# UNO_DATABASE_PASSWORD=
